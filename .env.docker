# Docker Environment Configuration for Hue MCP Server
# Copy this file to .env and customize the values for your setup

# Required: Your Hue Bridge IP address
HUE_BRIDGE_IP=192.168.1.64

# Required: Your Hue Bridge username (use setup.py to generate)
HUE_USERNAME=your_username_here

# Optional: Logging level
LOG_LEVEL=INFO

# Optional: HTTP client timeouts (seconds)
HUE_TIMEOUT_CONNECT=5.0
HUE_TIMEOUT_READ=10.0

# Optional: Connection pooling settings
HUE_MAX_CONNECTIONS=10
HUE_MAX_KEEPALIVE=5

# Optional: Rate limiting (requests per second)
HUE_LIGHT_RATE_LIMIT=10
HUE_GROUP_RATE_LIMIT=1.0